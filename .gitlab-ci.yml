image:  microsoft/dotnet:2.1.302-sdk

before_script:
  - dotnet restore
  
pages:
  stage: deploy
  script:
  - mkdir public
  - dotnet publish -c Release -o output
  - cp -a ./BlazorCVSite/output/BlazorCVSite/dist/* ./public
  artifacts:
    paths:
    - public
  only:
  - master